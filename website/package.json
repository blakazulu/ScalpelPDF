{
  "name": "scalpel-website",
  "private": true,
  "version": "1.0.0",
  "description": "Marketing website for Scalpel — the local-only, portable Windows PDF editor.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test": "playwright test",
    "test:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.1.0",
    "@fontsource-variable/geist-mono": "^5.1.0",
    "@fontsource-variable/heebo": "^5.1.0",
    "lottie-web": "^5.12.2"
  }
}
